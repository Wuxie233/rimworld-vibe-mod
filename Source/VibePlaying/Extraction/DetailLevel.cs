namespace VibePlaying
{
    public enum DetailLevel
    {
        /// <summary>~500 tokens. Colony headline numbers only.</summary>
        L0,
        /// <summary>~2K tokens. L0 + per-pawn summary + key resources.</summary>
        L1,
        /// <summary>~5K tokens. L1 + full details.</summary>
        L2
    }
}
